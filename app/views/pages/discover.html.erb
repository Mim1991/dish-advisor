<h2>Restaurants near me</h2>
<div class="discover-container">
  <div id="map"
       style="width: 100%; height: 600px;"
       data-markers="<%= @markers.to_json %>"
       data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
       data-center="<%= @coordinates.to_json %>">

  </div>
</div>

<!-- <div id="map"></div> -->
<!-- <script>
  console.log(document.getElementById("map").dataset.mapboxApiKey);
  mapboxgl.accessToken = document.getElementById("map").dataset.mapboxApiKey;
var map = new mapboxgl.Map({
container: 'map', // container id
style: 'mapbox://styles/mapbox/streets-v11',
center: [-96, 37.8], // starting position
zoom: 3 // starting zoom
});

// Add geolocate control to the map.
map.addControl(
new mapboxgl.GeolocateControl({
positionOptions: {
enableHighAccuracy: true
},
trackUserLocation: true
})
);
</script> -->

<h2 class="sub-heading">FEATURED DISHES</h2>
<div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <div class="row" id="dashboard-row">
        <% @dishes.sample(1).each do |dish| %>
          <div class="col-sm-12">
            <div class="dashboard-card">
              <div class="dashboard-infos" id="fav-infos">
              <div class="dish-title">
                <%= link_to dish.name.upcase, restaurant_path(dish.restaurant.id, anchor: "#{dish.name.parameterize}") %>
              </div>
                <div class="dish-description">
                  <p><%= dish.description %></p>
                </div>
                <div class="favourite-button-links">
                  <%= link_to dish_favourites_path(dish), method: :post, remote:true, class: "link" do%>
                    <i class="far fa-heart"></i>
                  <% end %>
                </div>
              </div>
              </div>
            </div>
        <% end %>
      </div>
    </div>
    <div class="carousel-item">
      <div class="row" id="dashboard-row">
        <% @dishes.sample(1).each do |dish| %>
          <div class="col-sm-12">
            <div class="dashboard-card">
              <div class="dashboard-infos" id="fav-infos">
              <div class="dish-title">
                <%= link_to dish.name.upcase, restaurant_path(dish.restaurant.id, anchor: "#{dish.name.parameterize}") %>
              </div>
                <div class="dish-description">
                  <p><%= dish.description %></p>
                </div>
                <div class="favourite-button-links">
                  <%= link_to dish_favourites_path(dish), method: :post, remote:true, class: "link" do%>
                    <i class="far fa-heart"></i>
                  <% end %>
                </div>
              </div>
              </div>
            </div>
        <% end %>
      </div>
    </div>
    <div class="carousel-item">
      <div class="row" id="dashboard-row">
        <% @dishes.sample(1).each do |dish| %>
          <div class="col-sm-12">
            <div class="dashboard-card">
              <div class="dashboard-infos" id="fav-infos">
              <div class="dish-title">
                <%= link_to dish.name.upcase, restaurant_path(dish.restaurant.id, anchor: "#{dish.name.parameterize}") %>
              </div>
                <div class="dish-description">
                  <p><%= dish.description %></p>
                </div>
                <div class="favourite-button-links">
                  <%= link_to dish_favourites_path(dish), method: :post, remote:true, class: "link" do%>
                    <i class="far fa-heart"></i>
                  <% end %>
                </div>
              </div>
              </div>
            </div>
        <% end %>
      </div>
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
